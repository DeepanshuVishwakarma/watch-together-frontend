// join room
// go live
// send request
// kick user
// join live

import styled from "styled-components";
import Button from "../UI/Button";
const JoinRoomButton = styled(Button)``;
const GoLiveButton = styled(Button)``;
const EndLiveButton = styled(Button)``;
const JoinLiveButton = styled(Button)``;

const KickUserButton = styled(Button)``;
const CreateRoomButton = styled(Button)``;
const SendRequestButton = styled(Button)``;
const UpdateRoomButton = styled(Button)``;
const DeleteRoomButton = styled(Button)``;
const UnsendRequestButton = styled(Button)``;
const RequestsRoomButton = styled(Button)``;
const AcceptRequestButton = styled(Button)``;
const DeleteRequestButton = styled(Button)``;

export {
  JoinRoomButton,
  GoLiveButton,
  JoinLiveButton,
  KickUserButton,
  UpdateRoomButton,
  SendRequestButton,
  DeleteRoomButton,
  CreateRoomButton,
  EndLiveButton,
  UnsendRequestButton,
  RequestsRoomButton,
  AcceptRequestButton,
  DeleteRequestButton,
};
